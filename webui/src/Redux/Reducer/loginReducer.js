import {createReducer} from '@reduxjs/toolkit'

const initialState = {}

export const loginReducer = createReducer(initialState,{
    login: (state,action) =>{
        
    },

    logout:(state)=>{
        
    }
})